MEMORY {
  PRG: start = $8000, size = $8000, file = %O, fill = yes, fillval = $EA;
}
SEGMENTS {
  CODE: load = PRG, type = ro, start = $8000;
  POLL: load = PRG, type = ro, start = $8D78;
  DATA: load = PRG, type = ro, start = $A000;
  VECTORS: load = PRG, type = ro, start = $FFFA;
}
